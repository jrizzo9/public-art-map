# Canonical site URL (no trailing slash). On Vercel, set explicitly to your subdomain.
NEXT_PUBLIC_SITE_URL=https://map.creativewaco.org

# Published Google Sheet CSV URL (Share → Publish to web → CSV).
SHEET_CSV_URL=

# Mapbox public token (client-side GL JS).
NEXT_PUBLIC_MAPBOX_TOKEN=

# Optional: Mapbox Studio style URL (the app defaults to the Creative Waco style if unset).
# NEXT_PUBLIC_MAPBOX_STYLE_URL=mapbox://styles/joshrizzocw/cmodpjicm00bv01scddibghqk

# ISR / fetch revalidation for sheet data (seconds). Use 0 for no fetch cache (always pull latest CSV).
REVALIDATE_SECONDS=300

# If true, attempt to geocode from address when lat/lng are missing (requires Mapbox token).
# GEOCODE_MISSING_COORDS=true

# Optional: image_id column → full image URL for map/popup/detail (replace {id}).
# NEXT_PUBLIC_ARTWORK_IMAGE_URL_TEMPLATE=https://cdn.example.com/images/{id}.webp

# Comma-separated origins allowed to iframe /embed/* (Webflow). Defaults to creativewaco.org + www.
# EMBED_ALLOWED_ORIGINS=https://creativewaco.org,https://www.creativewaco.org

# --- Optional: Admin sheet row updates ---
#
# Preferred: Direct Google Sheets API — when SHEET_ID + GOOGLE_SERVICE_ACCOUNT_JSON are set, POST /api/admin/sheet-row uses this (same setup as submissions).
# SHEET_ID=
# SHEET_ADMIN_RANGE='Live Sheet'!A:Z
# GOOGLE_SERVICE_ACCOUNT_JSON={"type":"service_account",…}
#
# Fallback: Apps Script Web App (doPost JSON { slug, patch, token }; token=SHEET_EDIT_API_TOKEN). Used only if SHEET_ID or GOOGLE_SERVICE_ACCOUNT_JSON is missing.
# SHEET_EDIT_API_URL=https://script.google.com/macros/s/…/exec
# SHEET_EDIT_API_TOKEN=
#
# Public submissions (`/submit`) require (B): each finalize appends one row to the `Submissions` tab (see scripts/submissions-sheet-header-row.csv). Override range with SHEET_SUBMISSIONS_RANGE='Submissions'!A:Z if needed.
#
# POST /api/admin/sheet-row has no caller secret — lock down the deployment if this URL is public.

# --- Admin UI (`/admin`) ---
# Plain password; session cookie is HTTP-only JWT (8h). Required for `/admin` and `/api/admin/*` except `/api/admin/auth`.
# ADMIN_PASSWORD=

# Public submit flow: set to `true` to enable the home “Submit” button and `/submit`. Omit or any other value = disabled.
# NEXT_PUBLIC_SUBMIT_ENABLED=true

# --- Optional: Cloudinary migration helpers (scripts only; do not expose in NEXT_PUBLIC_*) ---
# CLOUDINARY_CLOUD_NAME=
# CLOUDINARY_API_KEY=
# CLOUDINARY_API_SECRET=
# CLOUDINARY_FOLDER=public-art-map
