/** Canonical product label — keep in sync with navigation chrome and metadata. */

export const SITE_PRODUCT_NAME = "Public Art Map";

export const SITE_ORG_NAME = "Creative Waco";

export const SITE_METADATA_DEFAULT_TITLE =
  `${SITE_PRODUCT_NAME} · ${SITE_ORG_NAME}`;

/** Next.js metadata `title.template`; `%s` is replaced by segment titles. */
export const SITE_METADATA_TITLE_TEMPLATE = `%s · ${SITE_PRODUCT_NAME}`;
